# Base settings placeholder
# If you want to switch to `config.settings`, move/merge settings from `dineops-backend/settings.py` into this file.
# For now this file is intentionally minimal to avoid changing runtime behavior.

# Example: import everything from existing settings to keep current behavior
from cms_oder_online.settings import *  # noqa: F401,F403

