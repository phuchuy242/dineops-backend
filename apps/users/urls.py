# urls.py for apps.users
from django.urls import path

urlpatterns = [
    # Add app-specific URL patterns here when you create views
]
# serializers.py for apps.inventory
# Minimal template for Django REST Framework serializers.
# Replace MyModel with your actual model in this app when you're ready.

from rest_framework import serializers

# Example template (commented out until you add models):
# from .models import MyModel
#
# class MyModelSerializer(serializers.ModelSerializer):
#     class Meta:
#         model = MyModel
#         fields = '__all__'

